usage: jsx_manifest {OPTIONS} FILE -o OUTFILE

  concat extendscript files(*.jsx) with mainifest.[js|jsx]

  FILES is "manifest.js" or "manifest.jsx".

  OPTIONS:

    -o, --out        OUTPUT FILE.
    -b, --bind       Binding JSON DATA.
    -h, --help       Print this help message.
